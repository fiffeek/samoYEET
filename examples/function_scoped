void func() {
    int fun(int y) {
        void fun2() {
            if (y % 2 == 0) {
                yeet(y);
            }
        }

        fun2();

        if (y == 1) {
            return 1;
        }

        return y * fun(y - 1);
    }

    yeet(fun(10));
}

yeet(func());