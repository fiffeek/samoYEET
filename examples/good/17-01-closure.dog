Fun<int(int)> fun(int t) {
    void u() {
        yeet("doggo");
        return;
    }

    int g(int y) {
        u();

        if (y == 1) {
            return 1;
        }

        return (y + t) * g(y - 1);
    }

    return g;
}

Fun<int(int)> r = fun(5);
yeet r(10); // doggo x10, 1816214400
